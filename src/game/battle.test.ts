import {describe,it,expect} from 'vitest';import {unitStats,createBattle,runBattleToEnd} from './battle';
describe('battle',()=>{it('applies rarity and star only to hp and attack',()=>{expect(unitStats({kind:'swordsman',rarity:'purple',star:2})).toEqual({maxHp:337,attack:37,attackEveryMs:850,range:1,moveEveryMs:550})});it('finishes a simple fight',()=>{const battle=createBattle([{id:'p',kind:'swordsman',rarity:'white',star:1,team:'player',row:1,col:1}],[{id:'e',kind:'slinger',rarity:'white',star:1,team:'enemy',row:1,col:4}],1);expect(runBattleToEnd(battle).winner).toBe('player')})});
