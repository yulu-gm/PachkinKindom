import type{Rarity,RunState,Star,UnitKind}from'./model';import{transition}from'./run-state';import{rarityForKind}from'./unit-defs';export const COST={single:10,five:45}as const;export const RARITY_WIDTH:Record<Rarity,number>={white:34,blue:26,purple:20,orange:13,red:7};export const RARITY_MULTIPLIER:Record<Rarity,number>={white:1,blue:1.3,purple:1.7,orange:2.2,red:3};const SELL:Record<Rarity,number>={white:3,blue:5,purple:8,orange:12,red:20};
const rng=(seed:number)=>{let s=seed>>>0;return()=>((s=(s*1664525+1013904223)>>>0)/4294967296)};export const shuffleRarities=(seed:number):Rarity[]=>{const a:Rarity[]=['white','blue','purple','orange','red'],r=rng(seed);for(let i=a.length-1;i>0;i--){const j=Math.floor(r()*(i+1));[a[i],a[j]]=[a[j]!,a[i]!]}return a};export const purchase=(state:RunState,mode:keyof typeof COST)=>{if(state.phase!=='PREP')throw new Error('Not ready');if(state.units.some(u=>u.location==='recruit'))throw new Error('Recruit slot occupied');const cost=COST[mode];if(state.gold<cost)throw new Error(`Need ${cost} gold`);return{state:{...transition(state,'LAUNCHING'),gold:state.gold-cost,ballsFired:state.ballsFired+(mode==='single'?1:5)},count:mode==='single'?1:5,layout:shuffleRarities(state.seed+state.ballsFired)}};export const stageReward=(stage:number)=>20+5*stage;export const sellValue=(u:{kind:UnitKind;star:Star})=>SELL[rarityForKind(u.kind)]*3**(u.star-1);
